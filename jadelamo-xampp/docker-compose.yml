version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: jadelamo-xampp_web_1
      APP_PORT: 80

  web:
    image: tomsik68/xampp:8
    restart: unless-stopped
    stop_grace_period: 1m

    environment:
      TZ: Europe/Madrid

    volumes:
      - ${APP_DATA_DIR}/www:/www
