version: "3.2"

services:
  sun-panel:
    image: hslr/sun-panel:beta
    container_name: jadelamo-sun-panel
    restart: always

    volumes:
      - ./conf:/app/conf
      - /var/run/docker.sock:/var/run/docker.sock

    ports:
      - "3002:3002"
